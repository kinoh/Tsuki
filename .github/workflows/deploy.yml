name: Deploy

on:
  push:
    branches:
      - main

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Prepare included files
      run: |
        touch src/service_account_key.json
    - name: Install dependencies
      run: sudo apt-get install -y cmake protobuf-compiler age
    - name: Store secrets
      env:
        PROMPT_PRIVATE_KEY: ${{ secrets.PROMPT_PRIVATE_KEY }}
        SERVICE_ACCOUNT_KEY: ${{ secrets.SERVICE_ACCOUNT_KEY }}
      run: |
        sudo mkdir -p /var/secrets/
        printf %s "$PROMPT_PRIVATE_KEY" | sudo tee /var/secrets/prompt_key
        printf %s "$SERVICE_ACCOUNT_KEY" > src/service_account_key.json
    - name: Decrypt prompt
      run: |
        age -d -i /var/secrets/prompt_key src/prompt/initial.txt.age > src/prompt/initial.txt
    - name: Install SSH key
      env:
        PRIVATE_KEY: ${{ secrets.DOCKER_HOST_PRIVATE_KEY }}
      run: |
        mkdir -p ~/.ssh
        printf %s "$PRIVATE_KEY" > ~/.ssh/id_rsa
        chmod 700 ~/.ssh/id_rsa
        eval $(ssh-agent -s)
        ssh-add ~/.ssh/id_rsa
        ssh-keyscan -H $(echo ${{ secrets.DOCKER_HOST }} | cut -d@ -f2) >> ~/.ssh/known_hosts
    - name: Build and deploy
      env:
        OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        WEB_AUTH_TOKEN: ${{ secrets.WEB_AUTH_TOKEN }}
        DIFY_SANDBOX_API_KEY: ${{ secrets.DIFY_SANDBOX_API_KEY }}
      run: docker compose up --build --detach
