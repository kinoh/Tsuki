services:
  app:
    image: tsuki-dev
    build: .devcontainer/
    command: []
    volumes:
      - .:/workspace:cached
      - devcontainer-data:/home/vscode:cached
  mumble-server:
    environment:
      MUMBLE_VERBOSE: true

volumes:
  devcontainer-data:
