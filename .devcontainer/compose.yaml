services:
  core:
    build:
      dockerfile: .devcontainer/Dockerfile
    image: node:22-bookworm
    command: ["sleep", "infinity"]
    environment: !reset []
    volumes:
      - .:/workspaces/tsuki:cached
      - ../tsuki-work1:/workspaces/tsuki-work1:cached
      - core-data:/data
      - sandbox-data:/memory:ro
    stdin_open: true
    tty: true
